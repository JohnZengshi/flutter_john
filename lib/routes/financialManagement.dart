import 'package:flutter/material.dart';

class financialManagement extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        title: Text('财务管理'),
      ),
      body: Center(
        child: Text('financialManagement'),
      ),
    );
  }
}
