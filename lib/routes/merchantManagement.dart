import 'package:flutter/material.dart';

class merchantManagement extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        title: Text('商户管理'),
      ),
      body: Center(
        child: Text('merchantManagement'),
      ),
    );
  }
}
