import 'package:flutter/material.dart';

class purchaseMall extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        title: Text('采购商城'),
      ),
      body: Center(
        child: Text('purchaseMall'),
      ),
    );
  }
}
