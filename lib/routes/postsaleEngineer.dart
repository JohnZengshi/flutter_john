import 'package:flutter/material.dart';

class postsaleEngineer extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        title: Text('售后工程师'),
      ),
      body: Center(
        child: Text('postsaleEngineer'),
      ),
    );
  }
}
