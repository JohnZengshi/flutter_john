import 'package:flutter/material.dart';

class view_03 extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Center(
      child: Icon(
        Icons.offline_bolt,
        size: 100,
        color: Colors.grey,
      ),
    );
  }
}
